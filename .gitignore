# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment variables — NEVER commit this
.env
.env.local
.env.*.local

# Vite
*.local

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*

# Serena MCP
.serena/

# Firestore backups — real client data. NEVER commit these.
backups/

# Backup launcher, contains a login. Copy BACKUP.bat.example instead.
scripts/BACKUP.bat

# Firebase service account key — full admin access to the database.
# Treat it like a master key: never commit, never email, never share.
serviceAccountKey.json
**/serviceAccountKey.json
*-firebase-adminsdk-*.json

# GPU render server — the API key lets anyone reach the render server, and
# test-*.mp4 are local smoke-test artifacts, never real content.
render-server/api-key.txt
render-server/test-*.mp4
